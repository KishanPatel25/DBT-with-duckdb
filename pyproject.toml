[project]
name = "dbt-with-duckdb"
version = "0.1.0"
description = "This is dbt project that compute with duckdb."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "dbt-core>=1.11.10",
    "dbt-duckdb>=1.10.1",
]
